public class Ejercicio2 {
    public class Maximo {
        public static long MaximoValor() {
            char maxChar = Character.MAX_VALUE;
            short maxShort = Short.MAX_VALUE;
            int maxInt = Integer.MAX_VALUE;

            return maxChar + maxShort + maxInt;
        }
    }
}
